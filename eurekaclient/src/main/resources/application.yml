server:
  port: 8011
spring:
  application:
    # 设置注册到注册中心的名字
    name: provider
eureka:
  client:
    service-url:
      # 注册中心的访问地址
      defaultZone: http://localhost:8761/eureka/
  instance:
    # 是否将当前服务的IP注册到注册中心
    prefer-ip-address: true