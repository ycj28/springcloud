server:
  port: 8761
eureka:
  client:
    # 注册中心不需要注册自己
    register-with-eureka: false
    # 是否要同步其他注册中心的数据
    fetch-registry: false
    service-url:
      # 设置访问注册中心的路径
      defaultZone: http://localhost:8761/eureka/